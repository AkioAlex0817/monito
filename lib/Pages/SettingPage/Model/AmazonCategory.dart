class AmazonCategory {
  String cat_id;
  String name;
  String context_free_name;
  String highest_rank;
  String product_count;

  AmazonCategory(this.cat_id, this.name, this.context_free_name, this.highest_rank, this.product_count);
}
